export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  title: "Isolashes | Salón de Pestañas, Cejas y Labios",
  description:
    "Somos una empresa dedicada al servicio de las extensiones de pestañas. Ofrecemos una amplia variedad de productos para la aplicación de pestañas. Contamos con una academia de aplicación de pestañas y micropigmentación de cejar, labios, depilación de cejas, hidratación de labios. Estamos ubicados en Santiago, República Dominicana. Salón de Pestañas. Salón de Belleza.",
  links: {},
};

export type AppointmentConfig = typeof appointmentConfig;

export const appointmentConfig = {
  title: "Agenda tu cita - Isolashes",
  description:
    "Agenda tu cita con nosotros y disfruta de nuestros servicios en Isolashes.",
};

export type ContactConfig = typeof contactConfig;

export const contactConfig = {
  title: "Contáctanos - Isolashes",
  description:
    "Contáctanos para más información sobre nuestros servicios en Isolashes.",
};

export type ServicesConfig = typeof servicesConfig;

export const servicesConfig = {
  title: "Servicios - Isolashes",
  description:
    "Conoce nuestros servicios en Isolashes, desde extensiones de pestañas, depilación de cejas, micropigmentación de cejas, labios y más.",
};

export type AboutConfig = typeof aboutConfig;

export const aboutConfig = {
  title: "Sobre Nosotros - Isolashes",
  description:
    "Conoce más sobre nosotros en Isolashes, desde nuestra historia, misión, visión, valores y más.",
};

export type StoreConfig = typeof storeConfig;

export const storeConfig = {
  title: "Tienda - Isolashes",
  description:
    "Visita nuestra tienda y disfruta de nuestros productos en Isolashes, desde blister de pestañas, pinzas, adhesivos hasta bonder, primer y removedor.",
};

export type UserConfig = typeof userConfig;

export const userConfig = {
  title: "Perfil - Isolashes",
  description:
    "Conoce más sobre tu perfil en Isolashes, desde tus citas, ordenes, perfil y más.",
};

export type ProductsConfig = typeof productsConfig;

export const productsConfig = {
  title: "Productos - Isolashes",
  description:
    "Conoce nuestros productos en Isolashes, desde blister de pestañas, pinzas, adhesivos hasta bonder, primer y removedor.",
};

export type CheckoutConfig = typeof checkoutConfig;

export const checkoutConfig = {
  title: "Checkout - Isolashes",
  description:
    "Revisa tu carrito en Isolashes, desde tus productos, total a pagar y más.",
};
